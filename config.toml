baseURL = 'http://ejmastnak.com/'
languageCode = 'en-us'
title = 'ejmastnak'
disableKinds = ['taxonomy', 'term', 'section']

[markup]
  [markup.highlight]
      noClasses = false
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

enableGitInfo = true
